/**
 * @name Client
 */
// yeah im doing that continue
interface ClientOptions {
    token?: string;
}

export default class Client {
    private const token: string;
    
    constructor(options: ClientOptions) {
        
    }
};

// where is the chat lol
// go to the liveshare menu, and at the bottom there will be a chat, click it
// in Chat Channels